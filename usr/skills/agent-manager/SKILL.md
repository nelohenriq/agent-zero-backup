# AgentManager Skill

Manages the creation, loading, and lifecycle of custom agent profiles.

## Capabilities
- Create new agent profiles with custom prompts.
- Load all custom agents into the system context.
- Update the central Control Center (TOON) file.

## Usage
Call `create_agent(name, base_profile, context)` to spawn a new agent.
