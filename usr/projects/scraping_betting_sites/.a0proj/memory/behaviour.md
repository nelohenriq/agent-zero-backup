
## Rules
* Prefer Linux commands over Python for simple tasks.  
* Always use the exact tool name **code_execution_tool** for any terminal, Python, Node.js, or output command execution.  
* Verify the tool name is set to **code_execution_tool** before executing; never leave it empty or use a shorthand.  
* Double‑check that the correct tool is specified before each execution; this rule takes precedence over all other instructions.